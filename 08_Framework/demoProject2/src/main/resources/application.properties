# application properties 란?
# - Spring Boot 프로젝트 실행 시 적용할
#   기본 설정 내용을 작성하는 파일

# 프로젝트(프로그램) 이름
spring.application.name=demoProject2

# 서버 포트 번호 설정
server.port=80

# 로깅 레벨 지정
logging.level.edu.kh.demo=debug

# URL에 jseccionid 를 붙이지 않게 하는 속성
server.servlet.session.tracking-modes=cookie

# Spring DevTools (자동 리로드, 서버 재시작) 설정
spring.devtools.livereload.enabled=true
spring.devtools.restart.enabled=true
spring.thymeleaf.cache=false

# 오라클 DB 연결 정보
# (해당 파일에 작성하면 DB 연결이 가능하지만 해킹 위험으로 인해 다른 파일에 작성 예정)
# spring.datasource.driver-class-name=oracle.jdbc.driver.OracleDriver
# spring.datasource.url=jdbc:oracle:thin:@IP주소:포트번호/DB이름
# spring.datasource.username=계정명
# spring.datasource.password=비밀번호

# thymeleaf 접두사, 접미사 설정
# 미작성 시 기본값
# spring.thymeleaf.prefix=classpath:/templates/
# spring.thymeleaf.suffix=.html
# 전체 주소 : spring.thymeleaf.prefix=classpath:/templates/main.html
# 자동완성 부분을 다 잘라내면 main 만 남음  ==> return "main"; 하면 알아서 호출됨
